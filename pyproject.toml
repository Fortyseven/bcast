[project]
name = "bcast"
version = "0.1.0"
description = "Broadcast messages to multiple social media outlets"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mastodon-py>=1.8.1",
    "requests>=2.32.3",
    "rich>=13.9.4",
]
